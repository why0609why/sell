spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    username: sell
    password: sell
    url: jdbc:mysql://209.222.30.249/sell?characterEncoding=utf-8&useSSL=false
  jpa:
    show-sql: true

server:
  servlet:
    context-path: /sell

# 第一种配置日志的方法，配置的比较简单
#logging:
#  pattern:
#    # 控制台的日志的打印格式
#    console: "%d - %msg%n"
#  # 日志的文件
#  file: spring.log
#  # 日志打印等级
#  level:
#    com.iooc.LoggerTest: debug